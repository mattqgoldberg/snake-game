{
    "version": "2.0.0",
    "configurations": [

        {
            "name": "Launch App",
            "type": "lldb",
            "request": "launch",
            "cwd": "${workspaceFolder}/bin/",
            "program": "${workspaceFolder}/bin/app",
            "stopOnEntry": true
        }
    ]
}
